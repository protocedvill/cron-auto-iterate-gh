# Auto-iteration backlog

- [x] Add a pytest-based unit test suite covering lib/backlog.py, lib/guardrails.py, lib/config.py, and lib/state.py (currently zero tests exist in the repo despite prompts/implement_task.md asking the agent to "add or update tests if the project has a test suite").
- [x] Fix the inconsistent default forbidden-path pattern in lib/config.py: `.env*` (unlike `.git/**`, `**/*.pem`, `**/id_rsa*`, `**/secrets/**`) has no `**/` prefix, so fnmatch only blocks a top-level `.env` file and silently misses nested ones like `backend/.env` - change it to `**/.env*`.
- [x] iterate.py's version_info() only sha256-hashes iterate.py itself, not the lib/ package, even though deploy.sh syncs lib/ and prompts/ separately - a lib-only code change won't be reflected in the logged version hash. Extend version_info() to also hash lib/*.py (and ideally prompts/*.md).
- [x] lib/state.py's next_repo_index() does a read-modify-write of state.json with no file locking; if two invocations of iterate.py ever overlap (manual run during a timer tick, or a hung previous run), the round-robin pointer can race. Add a simple flock-based lock around the read-modify-write.
- [ ] lib/agent.py's render_prompt() uses str.format(**kwargs) on the raw template file contents, which will raise if a prompt template (e.g. prompts/implement_task.md) is ever edited to include a literal `{` or `}` (such as a JSON or code snippet). Switch to a safer substitution (e.g. string.Template or explicit str.replace) so template edits can't silently break prompt rendering.
